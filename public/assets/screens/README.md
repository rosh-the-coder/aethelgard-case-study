# Screen captures

Drop real product screenshots here using these basenames (any of `.webp` `.png` `.jpg` `.svg`):

| File | Screen |
|---|---|
| `dashboard-overview` | Factory Dashboard (hero LinkedIn shot) |
| `research-screen` | Research |
| `generation-screen` | Generation |
| `selection-screen` | Winner selection |
| `mockup-studio` | Mockup Studio |
| `seo-editor` | SEO editor |
| `etsy-draft` | Etsy draft |
| `print-output-view` | Print outputs |
| `folder-output` / `artifact-folder-anatomy` | Product folder |
| `provider-routing` | Provider routing |

SVG placeholders ship by default. When a real capture is added, the case study picks it up automatically (webp/png preferred over svg).
